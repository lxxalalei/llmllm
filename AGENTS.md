# AGENTS.md

## 项目目标

构建企业产品知识编译与问答平台。长期知识资产是第一优先级，Agent/Workflow 框架只是可替换基础设施。

## 开发约束

1. 优先实现真实业务闭环，不为了“架构完整”增加无业务价值的抽象。
2. 不把 LangGraph State、Agent Memory、Qdrant Vector 当作长期知识资产。
3. 不新增 CrewAI/AutoGen 等多 Agent 框架，除非出现明确且无法由现有 workflow 解决的需求。
4. 不为未知风险预埋复杂校验、哈希链、冗余状态机或 fallback。
5. 错误应尽量暴露真实原因，不以静默 fallback 掩盖问题。
6. 修改业务行为时先更新/新增业务测试，再实现代码。
7. 测试应验证用户/业务行为，而不是只验证内部实现细节。
8. 小修改优先运行相关测试，不默认每次跑全量测试。
9. 框架代码与领域代码分离，领域模型不得依赖 LangGraph。
10. 知识层级和权限策略必须由代码显式控制，不依赖 Prompt 自觉。

## 当前技术基线

- Python / FastAPI
- Pydantic
- LangGraph
- Tree-sitter
- PostgreSQL
- Qdrant
- Git + Markdown/YAML

## 项目事实来源

- `docs/PRD.md`：产品目标、范围、非目标和产品验收口径。
- `docs/architecture.md`：目标架构、系统边界和存储职责。
- 代码、配置、测试和实际运行结果：当前已经实现的行为。
- `docs/roadmap.md`：唯一的项目路线索引、当前主路线和下一验收项。
- `docs/plans/`：仅保存需要跨会话延续的有界实施计划及其归档；不得复制 roadmap 或代码可直接确认的状态。

这些来源冲突时，不得静默覆盖：先记录冲突、影响和待确认决定。历史计划或交接记录不能覆盖代码与运行时所体现的当前状态。

## 开发循环

1. 开始任务前检查分支、上游、工作区、适用规则和当前主路线。
2. 小任务直接记录目标、非目标和最小验证；只有跨模块或跨会话任务才在 `docs/plans/` 建立计划。
3. 当前 Agent 只推进一条主路线。并行路线必须标明所有者、文件范围、依赖和独立验收条件。
4. 实现前确认调用方、测试和真实行为；实现后检查实际 diff，并运行与风险相称的最小充分验证。
5. 验证记录必须写明命令、结果、证明范围和未覆盖项。单元测试、mock 或接口直调不得描述成真实用户链路。
6. 完成后更新 `docs/roadmap.md` 的状态和下一验收项；跨会话计划按 `docs/plans/README.md` 收口或归档。
7. 每个计划级或迭代级任务完成后执行“下一轮规划检查点”：只有用户已指定后继任务，或验收证据只导向一个明确后继目标时才建立下一计划；存在多个产品、架构或优先级方向时，记录候选并等待用户选择；没有具体后继目标时明确记录当前无下一轮计划。
8. 未经用户明确授权，不提交、推送、发布或改写 Git 历史。

子目录中的 `AGENTS.md` 可以增加更具体的规则；冲突时，离被修改文件最近的规则优先。

## 第一阶段禁止扩展

除非任务明确要求，否则不要主动加入 Kubernetes、Kafka、Neo4j、Redis、CrewAI、AutoGen、LlamaIndex、Haystack、复杂 Agent Memory 或多层 Repository/Service 抽象。新增依赖前先证明现有技术不能直接解决。
